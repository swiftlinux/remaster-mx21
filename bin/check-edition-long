#!/bin/bash

# NOTE: set -o pipefail is needed to ensure that any error or failure causes the whole pipeline to fail.
# Without this specification, the CI status will provide a false sense of security by showing builds
# as succeeding in spite of errors or failures.
set -eo pipefail

# This script checks for a mention of the full name of the special edition in a given file.

FILENAME=$1

source definitions.sh

if [ "$EDITION_SHORT" = 'hannahmontana' ]
then
  bin/error-if-snif 'Hannah Montana Linux' "$FILENAME"
else
  bin/error-if-snif 'Swift Linux' "$FILENAME"
fi  
