#!/bin/bash

# NOTE: set -o pipefail is needed to ensure that any error or failure causes the whole pipeline to fail.
# Without this specification, the CI status will provide a false sense of security by showing builds
# as succeeding in spite of errors or failures.
set -eo pipefail

STR1=$1
STR2=$2
DIRECTORY=$3

# This script replaces incidences of one string (STR1) with another (STR2)
# in all files within a directory (DIRECTORY).
echo "Replacing '$STR1' with '$STR2' in all files within $DIRECTORY"
for FILE1 in $(grep -rnwl -I "$DIRECTORY" -e "$STR1");
do
  bin/replace-sif "$STR1" "$STR2" "$FILE1"
done;
