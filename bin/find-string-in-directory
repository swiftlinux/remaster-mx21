#!/bin/bash

# NOTE: set -o pipefail is needed to ensure that any error or failure causes the whole pipeline to fail.
# Without this specification, the CI status will provide a false sense of security by showing builds
# as succeeding in spite of errors or failures.
set -eo pipefail

STR1=$1
DIRECTORY=$2

# This function lists all incidences of a string in all files within a directory

# -I flag: omits binary files from the results
# -r flag: recursive
# -n flag: show the line number
# -w flag: match the whole word
# -e flag: the pattern used during the search

echo '----------------------------------------------'
echo "BEGIN: searching for '$STR1' within $DIRECTORY"
echo '----------------------------------------------'
grep -rnw -I "$DIRECTORY" -e "$STR1"
echo '-------------------------------------------------'
echo "FINISHED: searching for '$STR1' within $DIRECTORY"
echo '-------------------------------------------------'
